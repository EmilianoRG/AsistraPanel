RewriteEngine On
# If the request is for an existing file or directory in project root, serve it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise rewrite everything to the web/ subfolder (internal)
RewriteRule ^(.*)$ web/$1 [L]

